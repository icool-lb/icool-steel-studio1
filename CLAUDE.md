# iCOOL Steel Studio — سياق المشروع لـ Claude Code

## ما هو
منصة ويب بملف واحد (`index.html`) لدراسة الهياكل الحديدية: برغولا · هياكل طاقة شمسية · هنغارات.
بدون backend، بدون build، بدون متغيرات بيئة. three.js r128 من cdnjs.

## المالك
iCOOL Trading & Contracting — م. أحمد محمد ناصر (OEA 36359)
GitHub: `icool-lb` · Vercel team: `icool-lb's projects`

## البنية
```
index.html    كامل التطبيق (HTML + CSS + JS) — لا تقسّمه إلى ملفات إلا بطلب صريح
vercel.json   cleanUrls + security headers
README.md     توثيق عربي
test/         اختبار Playwright + محاكي three.js + نموذج KML
```

## قواعد التعديل
- كل التعديلات داخل `index.html`. الواجهة RTL عربية بالكامل.
- ألوان الهوية: أزرق `#0066A7` وبرتقالي `#F1471E`. الشعار SVG مضمّن ويجب أن يبقى `direction:ltr` وإلا تتراكب النصوص في سياق RTL.
- اتجاه ميل الألواح: دوران الصندوق حول X بزاوية θ ينقل +Z إلى (0,−sinθ,cosθ). مستوى صاعد ⇒ θ=−t، هابط ⇒ θ=+t. الكود يمرّر `pt.dir` والدوران `-pt.dir*t`. لا تعكس الإشارة.
- الرياح: يجب الإبقاء على الفصل بين `cf` (تصميم الإطار) و`cp,net` (المثبتات). خلطهما يعطي تصميماً خاطئاً في الاتجاهين.
- أي مقطع جديد يُضاف إلى جدول `SEC` بوزنه kg/m ومساحته cm² وأبعاده بالمتر.
- كل عنصر يُرسم عبر `addM(kind, sec, p1, p2, mat, mark)` حتى يدخل جدول الكميات وقائمة القطع تلقائياً.

## المصفوفات المتعددة
- `ARR` قائمة الهياكل و`AI` فهرس النشط. الحقول في `ARRF` تُنسخ بين `M` و`ARR[AI]` عبر
  `syncToArr()` و`applyArr()` — أي أن كل واجهة التصميم تعمل على المصفوفة النشطة دون تغيير.
- `build()` يمرّ على كل مصفوفة: يطبّق حقولها على `M`، يشغّل البنّاء نفسه، يحسب `compute` لها وحدها،
  ثم ينقل ما رُسم إلى مجموعة ملفوفة تُدار بـ `arrPhi(a)` وتُزاح إلى موضعها. **لا تعدّل البنّائين.**
- `arrPhi(a) = (180 − (northDeg() + a.az))·π/180` — عند `az=180` (جنوب) و`north=0` تكون الزاوية صفراً
  فيبقى السلوك مطابقاً للنسخة أحادية الهيكل تماماً.
- الإزاحة نسبية إلى **المصفوفة النشطة** `(a.x−ox, a.z−oz)` لأن `drawSite()` يرسم السطح مزاحاً
  بـ `−bOX,−bOZ`. تغيير أحدهما دون الآخر يفصل الهيكل عن السطح.
- `LEGS` تُخزَّن **بإحداثيات السطح بعد الدوران** `[X,Z,fix,arrIdx,localZ]`، لذا `legState` لا تضيف
  `bOX` بعد الآن. `localZ` تحدّد الصف الأمامي/الخلفي.
- `R` = نتيجة المصفوفة **الحاكمة** (أعلى استغلال) مع استبدال الحقول التجميعية (الوزن، المساحة،
  الكلفة، kWp) بمجاميع كل المصفوفات. `ARES` يحمل نتيجة كل مصفوفة على حدة.
- `NOGRND` يمنع تكرار مستوى الأرض للمصفوفات الثانية فصاعداً.

## الاتجاه والظلال (وحدة KML)
- اصطلاح المحاور: **+X شرق · −Z شمال · +Z جنوب** عندما `north=0`. الشمال هو `bearing(0)=(sin θ, −cos θ)` حيث `θ=northDeg()`.
- `northDeg() = M.north + (bShape==='kml' ? bRot : 0)` — تدوير المخطط يدوّر البوصلة معه فيبقى الظل صحيحاً. لا تفصل الاثنين.
- إسقاط KML من (lon,lat) إلى المتر: `x=(lon−lon0)·mLon` و `z=−(lat−lat0)·mLat` — لاحظ **إشارة السالب على z** لأن الشمال −Z. عكسها يقلب الظل.
- الظل يقع في اتجاه **معاكس** لاتجاه الشمس: `shadow = −(h/tan(alt))·sunHoriz()`. `sunHoriz()` يشير **نحو** الشمس.
- `OBS` بإحداثيات ما قبل التدوير، و`OBSW` بعده — `makeSite()` وحدها تولّد `OBSW`. استعمل `OBSW` في الرسم والحساب، و`OBS` في التخزين والتحرير.
- نسبة التظليل تُحسب على **مستوى الألواح** عبر `panelY(z)` لا على مستوى السطح؛ الفرق جوهري لأن العمامة نادراً ما تظلّل ألواحاً على ارتفاع 2+ م.
- KMZ غير مدعوم (ملف مضغوط ويتطلب مكتبة إضافية) — المطلوب Save Place As → `.kml`.
- العوائق لها `h` (منسوب القمّة **فوق سطحنا**) و`base` (منسوب القاعدة، سالب = تحتنا). المباني
  المجاورة `kind:'bldg'` تُرسم من الأرض لكن التظليل يعتمد على `h` وحده. `h` سالبة = أخفض منّا فلا تظلّل.

## الطاقة والبطاريات
- `yieldArray(a,kwp)` محاكاة ساعية على يوم ممثّل لكل شهر (أيام Klein): الإشعاع اليومي ⇐ ساعي
  (Collares-Pereira & Rabl للكلي، Liu & Jordan للمنتشر)، الفصل بعلاقة Erbs، الإسقاط بنموذج HDKR،
  حرارة الخلية بـ NOCT. **لا تستبدلها بمعادلة سنوية مبسّطة** — التظليل يُحسب داخل الحلقة الساعية.
- السمت في الحساب وفي المجسّم يجب أن يأتيا من `azOf(a)` وحدها. **لا تكتب `+a.az||180`**:
  السمت صفر (شمال) قيمة صحيحة و`||` يبتلعها فيقلبها جنوباً ويجعل الحساب يناقض المجسّم.
  القاعدة نفسها تنطبق على كل حقل قد يكون صفراً مشروعاً (`base` للعوائق مثلاً).
- متجه عمودي اللوح ومتجه الشمس كلاهما في إطار ENU‏ `[شرق، شمال، أعلى]`، و`cosθ` جداءٌ قياسي —
  هذا يتجنّب اصطلاحات نصف الكرة. لا تحوّلهما إلى صيغة الزوايا المباشرة.
- `shFac` يضاعف أثر المساحة المظللة لأن التظليل الجزئي على سلسلة يخسر أكثر من نسبة مساحته.
  القيمة ليست فيزياء بل قرار هندسي — اتركها ظاهرة للمستخدم.
- لوحة الألوان في الرسم البياني `PVCOL` اجتازت فحص عمى الألوان (ΔE ≥ 10 للأزواج المتجاورة).
  أي لون جديد يجب أن يمرّ على نفس الفحص قبل إضافته.

## اختبار في متصفح حقيقي
```bash
npm i -D playwright && npx playwright install chromium
node test/e2e.js        # أو: CHROME_PATH=/path/to/chrome node test/e2e.js
```
`cdnjs` محجوب في بيئات التطوير المعزولة، لذا يعترض الاختبار طلب three.js ويستبدله بمحاكٍ مصغّر
(`test/three-mock.js`)، فيعمل كل المنطق غير الرسومي ويُختبر فعلياً: حسابات الشمس مقابل قيم
مرجعية لبيروت، قراءة KML، اتجاه الظل وطوله، بناء التقرير واللوحات الثماني.
الاختبار **لا** يغطي الرسم ثلاثي الأبعاد نفسه ولا خرائط الظل الحيّة (تحتاج three.js الحقيقي وGPU).

## اختبار سريع بدون متصفح
```bash
python3 - <<'PY'
import io
s=io.open('index.html',encoding='utf-8').read()
js=s.split('r128/three.min.js"></script>')[1].split('<script>')[1].split('</script>')[0]
io.open('/tmp/app.js','w',encoding='utf-8').write(js)
PY
node --check /tmp/app.js
```

## النشر
push إلى `main` → Vercel ينشر تلقائياً (بعد ربط المشروع بالريبو).
